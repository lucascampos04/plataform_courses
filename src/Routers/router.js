import { BrowserRouter, Route, Routes } from "react-router-dom"

export const Routers = () => {
    return(
        <BrowserRouter>
            <Routes>
                <Route path="/"/>
            </Routes>
        </BrowserRouter>
    )
}